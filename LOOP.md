# LOOP.md — agent-written iteration log

One line per iteration, written by the agent as the loop runs: maker first, then what ran, what broke, what got fixed. Structured mirror of every line lives in `data/loop.json` — **which this app itself renders: the app is its own build log.** Open the [live site](https://ouroboros-phi.vercel.app/), click any ring segment, and you're reading this file with the checker's evidence attached.

Every line carries labeled provenance you can cross-check: `commits:` are git SHAs in this repo (feature / fix / bank), `test:` is a TestSprite test id whose **full run history is committed** at `.testsprite/results/iter<N>-<id8>.json`, and failures link a lesson in [LESSONS.md](LESSONS.md) recorded *before* the fix (**no fix without a lesson**). Where the platform's status flag contradicted its own passing evidence, the verdict is labeled **pass-by-evidence** and the discrepancy was reported upstream (**the loop audited its checker** — [testsprite-cli#208](https://github.com/TestSprite/testsprite-cli/issues/208)). The AUDIT TRAIL section of the live app renders this same mapping as a table.

---

1. [claude] Baseline instrument panel (ring + stats + iteration log) — test "Homepage renders the loop instrument panel" passed 6/6 steps — nothing broke — commits: feature 56c5b00, bank 61faf97 · test bd2aa814 · verdict passed.
2. [claude] Interactive ring (click a segment → iteration story card) — test "Clicking a ring segment reveals that iteration's detail card" FAILED: the tester saw no clickable arcs (svg role="img" flattened the a11y subtree; segments not keyboard-focusable) — lesson L1 banked, fixed with real accessible controls — rerun passed — commits: feature 3ef33c0, fix 0d22d23, bank 9ccbacc · test 209afd81 · failure bundle `.testsprite/failure/` · verdict passed.
3. [codex] Motion tokens + GSAP ring draw-on intro — test "Ring intro completes and leaves all segments visible" FAILED on an over-strict assertion that the orbit dot must visibly move (reduced-motion browsers make that correct behavior; screenshot-sampled motion checks are flaky) — lesson L2 banked, plan refined to settled end-state assertions — rerun passed — commits: feature 57e2a53, bank 9bd3d06 · test 86ccc90d · verdict passed.
4. [codex] Flight-recorder replay (RAF clock, scrubber over a native range input, diamond markers; ring/log/stats derive from the replay position) — test "Replay transport rewinds and replays the loop history" passed 21/21 steps first try — commits: feature 1b5f053, bank 5d02e43 · test 8ba3267b · verdict passed.
5. [codex] Learning curve (stacked pass/fail run bars + cumulative lessons step-line + lesson rows) — test "Learning curve chart shows per-iteration runs and banked lessons": every step green twice, but the platform returned status "blocked" while its own conclusion said PASS — lesson L3 banked (audit the checker's verdict against its observations), reported upstream — commits: feature 754433b, bank cf0689b · test 285783c3 · verdict pass-by-evidence, later run flagged passed.
6. [codex] Harness cycle diagram (WRITE→VERIFY→BANK with the fail→LESSON loop) — test "Harness diagram explains the loop topology" FAILED: the 11px 'pass' connector label was unreadable in screenshots — lesson L4 banked, labels raised to 13.5px — rerun verified every item incl. the label (status flag showed the blocked-with-PASS bug again; appended upstream) — commits: feature c3b4fb1, fix 3773608, bank 3d11ec8 · test e148ba09 · artifact `.testsprite/runs/t6-blocked/` · verdict pass-by-evidence, later run flagged passed.
7. [codex] Checker evidence inside story cards (failure-bundle excerpts: run id, root cause, fix target) — test "Iteration story card surfaces the checker's failure evidence" FAILED: the tester clicked the replay diamond ("Seek to iteration 2") instead of the ring arc (same-entity look-alike controls) — lesson L5 banked, accessible names diverged + plan names the widget type — rerun passed 17/17 (run 4c40bad3, in the committed history) — commits: feature b265f6e, fix 1b9d750, bank 3d11ec8 · test 215f4e47 · artifact `.testsprite/runs/t7-fail/` · verdict passed.
8. [codex] Polish: OG meta + favicon + footer + hook line + mobile pass (5 real overflow bugs found at 375px, fixed) — test "Page chrome orients a first-time visitor" passed — commits: feature 017ac75, bank 1076935 · test fa60727a · verdict passed.
9. [claude+codex] Growth regression — at 8 iterations the per-segment intro left arcs invisible 3+ seconds; the tester indexed mid-intro and found no clickable ring, then missed SVG-child controls intermittently even when visible — lesson L6 banked (cap data-driven entrances at a total budget; intro now 0.9s max) and controls hardened into real HTML button hit-targets (L1 updated) — this iteration hardened the same ring/story-card feature as iteration 7, so it is verified by re-running that feature's test 215f4e47; the latest run is a pass (see `.testsprite/results/iter9-215f4e47.json`) — commits: fix e80e947 + 7105515, bank 1076935 · test 215f4e47 (shared with iteration 7) · verdict passed.
10. [claude+codex] Trust audit — a hostile review (by a second agent) cross-examined this file against git and the committed artifacts and found the greens under-evidenced (only failures had committed bundles), the ring visually hiding its failures (all arcs green), a stale story card surviving replay scrubs, an a11y label conflict, sub-AA muted contrast, and CI not proving it tested the fresh deploy — fixed: full run histories committed for all tests (`.testsprite/results/`), red failure-rims, stale selection cleared, contrast raised, and CI now waits until the live site serves the exact commit SHA before re-running the suite — honest note: this iteration's bank commit 08c0b17 actually FAILED CI on a build error; the fix landed in dfb21f5, whose run is the first to pass the full SHA-gated suite against the live commit — commits: feature d23c1a7, fix dfb21f5, bank 08c0b17 · verified by CI on dfb21f5 · verdict passed.
11. [codex] Deep-linkable iterations — clicking a ring segment writes ?i=N (history.replaceState) and loading ?i=N opens that story card; a copy-permalink button shares it — test "Deep link opens an iteration's story card and offers a copy-link button" passed 4/4 first try — commits: feature baf38ab, bank 319e7e0 · test 877e0b8b · verdict passed.
12. [codex] Failures-only filter — one toggle isolates the caught-failure iterations across the ring, log, and stats (applied after the replay slice so it never reveals scrubbed-away history) — test "Failures-only filter narrows the loop to iterations that caught a failure" passed — commits: feature baf38ab, bank 319e7e0 · test 31d2747b · verdict passed.
13. [codex] Keyboard shortcuts overlay + full keyboard nav (the a11y capstone — the app dinged twice for a11y ends fully keyboard-navigable) — test "Keyboard shortcuts overlay opens and closes" FAILED: the "press ? for shortcuts" hint's aria-label overrode its visible text, hiding the "?" from the a11y-tree-reading tester — lesson L7 banked (third of the a11y trio: L1 hid controls, L5 confused names, L7 hid content), fixed by removing the masking label — rerun: the testing agent's own conclusion was PASS (platform returned "blocked" per issue #208 — pass-by-evidence) — commits: feature baf38ab, fix 319e7e0, bank 319e7e0 · test 0447a447 · verdict pass-by-evidence.
14. [codex] The presentation layer — Judge Mode (press J: a self-narrating 6-beat briefing that makes the app argue its own case, climaxing in "The Checker On Trial" — the checker's own "blocked" vs "PASS" contradiction, with links to issue #208 and PRs #207/#213), the ring's instrument glow, and a hero that finally states the idea ("the brief: build the loop → so we pointed the loop at itself", with a recursion arrow that loops back on itself) — test "Judge Mode runs a self-narrating briefing that puts the checker on trial" passed 21/21 first try; additive overlay, all prior tests still green — commits: feature 186eddc (+ glow 843d0bf + hero 808542f), bank ea6999c · test f6c69559 · verdict passed.
15. [codex] The invitation & the trophy — a prominent "start the 3-minute briefing" CTA (so judges actually find Judge Mode), the shareable Loop Card (a trophy graphic with the real proof numbers + a native-canvas "download PNG"), and Failure Fossils (clicking a red segment shows the exact committed broken state, preserved) — test "The invitation and the trophy" FAILED: the checker caught two accessibility regressions — the CTA's aria-label masked its own visible invitation (L7 again) and the Loop Card's text was SVG-only, invisible to the a11y tree — lesson L8 banked (content-SVG needs a real text equivalent; fourth a11y lesson) — fixed (CTA's masking aria-label dropped; the Loop Card given a real visible HTML caption) — rerun passed 4/4 — commits: feature fd60a40, fix 5f390b1, bank 1c7c772 · test 1af81937 · verdict passed.
